# 关系图鼠标缩放与拖拽

## 目标

- 关系图画布支持鼠标滚轮缩放。
- 按住鼠标左键拖拽画布时可以平移查看关系。
- 保留工具栏缩放按钮和重置操作。

## 实施

- 在 `GraphPage.vue` 增加滚轮、指针按下、移动、抬起事件。
- 使用缩放值与平移偏移共同控制 SVG 画布变换。
- 增加拖拽中的光标和触摸动作样式。

## 验证

- 前端 TypeScript 检查和生产构建通过。
- 检查鼠标拖拽不会触发关系节点或按钮的错误操作。
